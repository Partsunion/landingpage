name: Landing CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: landing-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Set up Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build static production site
        run: npm run build

      - name: Check search and AI discoverability
        run: npm run seo:check

      - name: Check product screenshots and topic mappings
        run: node --experimental-strip-types scripts/check-product-images.mjs

      - name: Check landing logic and static asset serving
        run: npm test

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high
